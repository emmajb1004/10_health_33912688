<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1 class="mb-4"></h1>
        <h2>Here are your search results</h2>
        <%# Show message if no results found for search query %>
        <% if (searchResults.length === 0) { %>
            <p>No events found matching your search.</p>
        <% } else { %>
            <% searchResults.forEach(function(search){ %>
                <div class="card mt-4">
                    <div class="card-body">
                    <h4 class="card-title"><%= search.title %></h4>
                    <div class="card-text mb-2">
                    <br>
                    <h6 class="mt-2">number of full price tickets left:</h6>
                    <%= search.ticket_type_full %>
                    <h6>number of discount price tickets left:</h6>
                    <%= search.ticket_type_discount %>
                </div>
                    <div class="card-subtitle text-muted mb-2">
                    <h4>event date:</h4>
                    <%= search.event_date %>
                    </div>
                    <a href="/attendee/eventDetails/<%= search.id %>"><button type="submit" class="btn btn-primary">More Info</button></a>
                </div>
                <% }) %>
            <% } %>
    </div>
</body>
</html>